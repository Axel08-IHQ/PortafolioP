---
import Layout from '../layouts/Layout.astro';
import Project from '../components/Project.astro';
import Sidebar from '../components/Sidebar.astro';
const baseUrl = import.meta.env.BASE_URL.replace(/\/$/, '') + '/';
const projects = [
  {
    title: "Proyectos Personales",
    desc: [
      "Proyectos personales y experimentos de código",
      "Próximamente más proyectos desde GitHub"
    ],
    tech: ["Python", "Node.js", "NPM"]
  }
];
---

<Layout title="Proyectos Personales | Axel Huera" description="Proyectos personales y proyectos de código abierto">
  <div class="content">
    <Sidebar selectedItem="personal-projects" />
    <main class="main">
      <div class="container">
        <div class="back-nav-container">
          <a href={baseUrl} class="back-nav-button">
            <img src={`${baseUrl}assets/back.svg`} loading="lazy" alt="Volver" class="back-nav-img" />
            <span>Volver</span>
          </a>
        </div>
        <div class="content-container">
          <h1>Proyectos Personales</h1>
          <div class="items-container">
            <div class="resource-items">
              {projects.map(project => (
                <Project 
                  title={project.title} 
                  desc={project.desc}
                  tech={project.tech}
                />
              ))}
            </div>
          </div>
        </div>
      </div>
    </main>
  </div>
</Layout>
